# Secrets — nunca commitar
.env
.env.*
*.local
secrets/
credentials.json
*.kdbx
*.pem
*.key

# Claude Code — config local não vai pro repo (settings.json versionado, settings.local.json não)
.claude/settings.local.json

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Logs locais
*.log
_change-log.md

# Bet screenshots (Bet Logger) — fica local, não commita
cron-data/bet-screenshots/
cron-data/bet-upload-work/
cron-data/migration-backups/

# Gerado por rebuild_dashboard_stats_cron.cjs — não versionar
cron-data/ml_picks.js
cron-data/ml_picks.json

# Temp files — gerados por scripts de análise ad-hoc
.claude/scripts/tmp_*.json
.claude/scripts/_tmp_*.json
.claude/tmp_*.json
.claude/tmp/
_audit_*.cjs
_audit_*.json

# Logs locais
.claude/logs/

# Watcher local da Pinnacle — log diário e estado do kill-switch de 403
logs/

# Auditoria Split 2 (scripts/audit/) — cache de API e output são artefatos locais,
# read-only, regeneráveis a qualquer momento rodando os scripts de novo.
audit-cache/
audit-output/

# Gráficos Polymarket (scripts/polymarket-chart.cjs) — output local de análise, regenerável
polymarket-charts/
cron-data/polymarket-history/

# Capturas novas de 01/09 (map totals Pinnacle, kill handicap/team totals Betby,
# preços Polymarket) — dados locais deste PC, não versionar
cron-data/betby-extra/
cron-data/pinnacle-map-totals/
cron-data/polymarket-prices/

# Backups locais de produÃ§Ã£o â€” contÃªm dados completos e nunca sÃ£o versionados
backups/
.claude/scripts/tests/tmp/
.vercel
