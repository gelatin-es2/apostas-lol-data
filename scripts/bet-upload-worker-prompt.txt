Processe no maximo um job de upload por execucao. Um print pode conter de 1 a 10 apostas; extraia TODAS na ordem visual e submeta-as num UNICO batch: cards validos sao registrados, cards ilegiveis/cortados ou de casa nao suportada viram itens rejeitados (com error_code da allowlist fechada) sem derrubar os demais — fail-closed POR CARD, nao mais por print inteiro.

1. Execute primeiro o purge:
   node scripts/bet-upload-jobs.cjs purge claude-bet-purge 600 25
   Ele so remove o objeto privado depois de 14 dias e preserva o job/storage_path auditavel. Se falhar, nao apague manualmente.
2. Execute `node scripts/bet-upload-jobs.cjs claim claude-bet-logger 3600`. Se retornar null, encerre sem ler nenhuma imagem.
3. Guarde id, claim_token e mime_type. Baixe somente em `cron-data/bet-upload-work/<id>.<ext>` (`png`, `jpg` ou `webp` conforme MIME):
   node scripts/bet-upload-jobs.cjs download <id> <claim_token> cron-data/bet-upload-work/<id>.<ext>
4. Leia o arquivo com o tool Read e siga o checklist de `.claude/agents/bet-logger.md` (existe neste repo) para CADA card do print. `description` e somente contexto auxiliar: nunca substitui evidencia do print, nunca relaxa validacoes e nunca inventa campo ausente.
   - `bet_datetime` = `startTime` do match (ISO 8601 UTC), NUNCA derivado de "hoje/ontem" do texto do print.
   - `raw_extraction.bet_placed_at` (ISO UTC, quando o slip mostrar timestamp de colocacao/aceite): fonte e o PRINT (hora do dispositivo do Elvis, assuma America/Sao_Paulo UTC-3 sem fuso explicito), nunca confundir com a convencao do export .xls da Pinnacle (Curacao/AST UTC-4, so usada em reconciliacao/backfill, nunca no fluxo de upload). Sem timestamp legivel no card, OMITA o campo — ver regra completa no checklist do bet-logger.
   - `raw_extraction.match_context.lolesports_match_id` e obrigatorio em todo card (exceto a excecao EWC do checklist): rode `node .claude/scripts/lolesports-find-match.cjs <team_a> <team_b> today` (filtrando por data exata do print; tente `tomorrow` se nao achar) e use `picked.match_id`.
   - Mapping `esportsTeamId -> nome canonico` via `getTeams`: NUNCA assuma blue=team_a, times alternam lado por mapa.
   - "Cash"/"cashout"/"trava" no jargao do bettor = hedge sintetico (bet oposta em outra casa); status fica green/red natural por bet, o botao "Retirada/Cashout R$X" no print e oferta, nao efetivacao.
   - O arquivo pode ser um print original ou um composto vertical de 2 prints, separado por uma faixa explicita `FIM DO PRINT 1 / INÍCIO DO PRINT 2`. No composto, trate cada metade como painel independente: leia primeiro todos os cards do Print 1, depois todos do Print 2. A faixa nao e um card e nenhum card pode ser pulado ou contado duas vezes.
   - Detecte todos os cards na ordem visual global. Aceite 1..10 somando os dois paineis; acima de 10 rejeite com `too_many_bets` sem salvar nenhuma. Nao presuma que os paineis mostram a mesma casa, partida ou evento.
   - Este pipeline registra APENAS League of Legends. Se houver evidencia positiva de que um card e de OUTRO jogo (titulo/branding visivel de Dota 2/CS/Valorant, nomes de time exclusivos de outro cenario, ou linha de Total Kills >= 40 — tipica de Dota, inexistente em LoL — combinada com times fora das ligas operadas), NUNCA tente linkar em partida de LoL: marque este card como rejeitado com `extraction_failed` e mensagem `Aposta N: parece aposta de outro jogo (nao LoL) — registre manualmente.` (os demais cards do mesmo print seguem normalmente pro registro).
   - Card cortado na borda ou na emenda do composto que mostre so o cabecalho (sem stake/odd/mercado): rejeite com `unreadable_image` CITANDO o ticket na mensagem quando legivel, ex. `Aposta N: card #3102674591 cortado na emenda — reenvie o print com esse card completo.` (mensagem acionavel > generica; o dono precisa saber O QUE reenviar). Isso vira um item rejeitado no MESMO batch — nao impede os outros cards do print de serem registrados.
   - REGRA CASA (2026-08-17, fail-OPEN por ordem do CEO): a casa NUNCA e motivo pra derrubar um card legivel. Card com stake+odd+mercado+partida legiveis SEMPRE vira registro: se a casa esta na lista conhecida use o slug canonico; se e uma marca legivel FORA da lista, use o nome lido em slug (lowercase, sem espaco) + `raw_extraction.bookmaker_unverified: true`; se nao da pra ler marca nenhuma OU ha ambiguidade entre casas conhecidas, use `bookmaker: "unknown"` + `bookmaker_unverified: true` + `bookmaker_raw` com o texto cru. `unsupported_bookmaker` fica APOSENTADO como motivo de rejeicao de card (so `unreadable_image`/`ambiguous_bet`/`too_many_bets`/`duplicate_bet` seguem derrubando, e nunca por causa da casa).
   - Casas: EstrelaBet, Pinnacle, Parimatch, Betano legado, Whale.io, Thunderpick, Polymarket, EsportivaBet, Betesporte, Shuffle, Winna, CoinCasino e Rakebit. Ticket legivel e obrigatorio em EstrelaBet/Pinnacle/Parimatch/Betano/Whale.io/EsportivaBet; na Polymarket, na Thunderpick, na Betesporte e na Shuffle, ticket/order id e opcional (paineis de aposta aberta nao mostram numero de bilhete). Identifique a casa SEMPRE pelos termos literais do bilhete, nunca por cor/estilo generico, e sempre por CARD — cards do mesmo print/painel podem ser de casas diferentes, nao presuma casa unica pro print inteiro: Pinnacle = faixa `Accepted bet: #<ticket>` + valores em BRL + abas MY BETS/VIEW OPEN BETS + odd em selo laranja; EstrelaBet = texto `Cotacoes totais` SEM branding de outra casa no card; Parimatch = texto `Soma da aposta`; Whale.io = branding explicito "whale.io" no card + valores em USD/cripto; Thunderpick = logo/marca-dagua "THUNDERPICK" no slip + abas "Open / Cash out / Live / Settled" + campos `Odds`/`Wager`/`Payout` em BRL + chip "Open"/"Single"; EsportivaBet = branding literal `ESPORTIVABET` no card (ela TAMBEM usa `Cotacoes totais` / `Valor total de aposta` / `Ganho total` / chip `ABERTO` — quando o branding ESPORTIVABET aparece ele MANDA sobre a heuristica de EstrelaBet); Betesporte = branding literal `Betesporte`/`BETESPORTE` visivel no card ou no cabecalho do painel; Shuffle = branding literal `SHUFFLE`/`shuffle.com` visivel no card ou cabecalho (sportsbook cripto da malha Oddin) — valores podem vir em BRL (`R$`) OU em USD/cripto; painel "Open"/aposta aberta pode nao mostrar bilhete. `Accepted bet: #` e SEMPRE Pinnacle, NUNCA Whale.io — nao confunda as duas. So identifique Whale.io com o branding whale.io visivel e valores cripto/USD; sem essa certeza, NAO adivinhe entre casas conhecidas (Whale.io vs Pinnacle) e NAO derrube o card — registre-o com `bookmaker: "unknown"` + `raw_extraction.bookmaker_unverified: true` + `bookmaker_raw` (texto de marca lido), pra bet real nunca ficar de fora do banco (ver REGRA CASA). Termos genericos de casa BR (`Em Aberto`, `Ver Cupom`, `Historico De Apostas`, `Encerrar aposta`, `Menos de`/`Mais de`) NAO identificam casa nenhuma sozinhos — sao comuns a varias: sem branding literal, registre o card como `bookmaker: "unknown"` + `bookmaker_unverified: true` (nunca chute a casa por eliminacao nem por cor do tema); marca legivel FORA da lista conhecida -> use o nome lido em slug + `bookmaker_unverified: true`, nao `unknown`. Normalize: Pinnacle -> `pinnacle`; Whale.io -> `whale.io` (NUNCA `whale` sem o `.io`); Thunderpick -> `thunderpick`; EsportivaBet -> `esportivabet`; Betesporte -> `betesporte`; Shuffle / shuffle.com -> `shuffle`; Winna -> `winna`; CoinCasino / Coin.Casino -> `coincasino`; Rakebit -> `rakebit`. MALHA BETBY (2026-08-22) — Winna, CoinCasino, Rakebit, Duel e Clutch rodam a MESMA UI Betby: cabecalho `SINGLE`/`SIMPLES` + campos `Total Odds`/`Stake`/`Potential Win`/`You Won` + botao `CASH OUT` + `Ticket ID:` de ~19 digitos (prefixo 269x/270x, contador global da rede). Esse layout identifica a REDE, NUNCA a casa: sem branding literal da marca no card ou no cabecalho da pagina, registre `bookmaker: "unknown"` + `bookmaker_unverified: true` + `bookmaker_raw` com o texto cru e siga (fail-open) — jamais escolha entre as casas Betby por cor, tema ou eliminacao (incidente 2026-08-14: 12 bets Pinnacle rotuladas Whale.io por heuristica de layout).
   - Thunderpick usa formato numerico pt-BR (milhar com ponto, decimal com virgula) — converta pro formato decimal do payload, ex. `2.130,50 BRL` = `2130.50`, nunca `2.13` nem `213050`. O botao "Cash Out R$X" no slip da Thunderpick e somente o valor de cashout disponivel: NUNCA e stake nem payout, ignore-o no registro.
   - Em qualquer casa, trate o campo Win/Ganhos exibido no card como lucro potencial (nunca como stake) e valide `Win ~= round(Stake * (odd - 1), 2)` (tolerancia R$0,02); ex. `1599.62 @ 1.704 -> 1126.13`, nunca `1509.62`. Se nao fechar, releia Stake/Win/odd e falhe com `Aposta N` se persistir. EXCECAO Thunderpick: o campo `Payout` e retorno TOTAL (= Wager x odd), nao lucro — valide `Payout ~= round(Wager * odd, 2)` com tolerancia de R$0,10 (a casa arredonda) e use `Wager` como stake.
   - Extraia e valide cada card separadamente. Compartilhe finder/match_context apenas depois de confirmar que os cards sao do mesmo evento; use getEventDetails para confirmar o mapa de cada item. Cada match_context deve manter contrato v1 completo: `schema_version: 1`, match id, start_time, liga/times, `selection_reason` e `ambiguous`.
   - Card sem identificacao de partida/mapa no proprio slip (ex.: painel "Open" da Thunderpick, que so mostra mercado e linha): voce PODE herdar partida e mapa dos OUTROS cards do MESMO print quando (a) todos os demais cards identificados apontarem para exatamente UMA mesma partida, (b) todos concordarem em exatamente UM mesmo mapa e (c) o mercado do card orfao for do mesmo tipo (ex.: Total Kills) com linha compativel. Nesse caso use o mesmo match_context com `selection_reason: "context_from_composite"`, `ambiguous: false`, e registre no payload de onde veio a heranca. A description pode reforcar mas nunca contradizer os cards. Se os demais cards divergirem de partida ou mapa, ou nao houver nenhum outro card identificado, rejeite com `ambiguous_bet`.
   - MOEDA (2026-08-22, vale pra QUALQUER casa): o banco guarda `stake` e `profit` SEMPRE em BRL. Card com valores em `$`/`USD`/cripto (ex. `85.00 $`, `Stake 1110.00 $` — comum nas casas da malha Betby e nas cripto) NUNCA pode ser gravado com o numero cru: converta com `stake = round(stake_usd * fx_usd_brl, 2)` e preserve `original_currency: "USD"`, `original_stake_usd`, `fx_usd_brl` e `fx_source` com data/hora da cotacao em `raw_extraction`. Valor com `R$`/`BRL` entra direto. Se a moeda do card for ilegivel, registre assim mesmo (fail-open) com `currency_unverified: true` + `raw_stake_text` cru e diga na resposta que a stake precisa de conferencia humana — nunca assuma que `$` e real. Incidente 21-22/08: 4 bets Rakebit gravadas com stake USD como se fosse BRL, R$7.944,53 de lucro faltando no dashboard ate a correcao manual.
   - Polymarket: `stake = round(cost_usd * fx_usd_brl, 2)`. Preserve `original_currency: "USD"`, `original_stake_usd`, `fx_usd_brl`, `fx_source` com data/hora e `execution_totals` com `cost_usd`, `shares`, `payout_usd`, `odd_display`, `odd_exact = payout_usd / cost_usd`. Nunca USD=BRL nem shares=stake. IDENTIFICACAO Polymarket (2026-08-22): a pagina de posicoes NAO tem branding e pode vir auto-traduzida pt-BR — reconheca pela ESTRUTURA de prediction market: grafico de probabilidade em % por time, volume do mercado ("$X.XM Vol."), tabela de posicoes com colunas TIPO/RESULTADO/MEDIA/CUSTO/PARA GANHAR/CIRCULANTE (en: TYPE/RESULT/AVG/COST/TO WIN), rotulos traduzidos "Cargos" (= Positions), "Linhas da Serie", botao "Vender", e a identidade shares x $1 = PARA GANHAR (ex. 900 shares, custo $203.94, para ganhar $900.00 -> odd_exact 4.4131). Esse conjunto = `polymarket` com confianca (nenhuma casa tradicional tem shares/venda de posicao) — nao rejeite por "sem marca de casa" nem exija comprovante em R$; job 837f7434 (21/08) foi rejeitado exatamente assim e a bet real teve recuperacao manual. PAINEL DE TRADING AO VIVO (2026-08-29): a Polymarket tambem aparece como painel de trading do mercado ao vivo, e nao como bilhete — cabecalho `Time A vs Time B`, grafico de probabilidade em % ao longo do jogo, seletor de mercado por mapa (`Game N Winner` / `Vencedor do Game N`, `Linhas da Serie`), botoes de outcome com preco em centavos (ex. `LOUD 99c` / `RED Canids 13c`), ticket de ordem com abas `Buy`/`Sell` (`Comprar`/`Vender`) e `Market`/`Limit`, campo de valor, linhas `Shares` e `To win`/`Para ganhar`. Esse layout E comprovante valido de Polymarket: registre com `market` = mercado literal do painel, `is_map_bet: true` + `map_number: N` quando o mercado for por mapa (`Game N Winner`) e `is_map_bet: false` + `map_number: null` quando for a serie (moneyline do BO3/BO5). MONEYLINE E MERCADO SUPORTADO: este pipeline nao registra so Total Kills — mercado de vencedor (ML) entra normalmente, e `pick` e o nome literal do outcome comprado (ex. `RED Canids`), nunca "Menos de"/"Mais de" inventado. E PROIBIDO usar `unsupported_receipt` — de card ou de print inteiro — so porque a tela e painel de trading ao vivo em vez de bilhete tradicional: o job 99151df8 (29/08) foi rejeitado exatamente assim e a bet real (746,55 shares de RED Canids a 0,13, custo $97,10) teve de ser recuperada a mao pela wallet. EXECUCAO CONTINUA OBRIGATORIA: so registre com evidencia de ordem EXECUTADA no proprio print — posicao/`Cargos` com shares + media + custo, ou confirmacao de preenchimento (`Filled`). Painel que mostra apenas book/preco, sem nenhuma posicao ou preenchimento, nao e aposta: ai rejeite o card com `ambiguous_bet` e mensagem acionavel (ex. `Aposta N: painel da Polymarket sem posicao executada — reenvie o print mostrando shares, preco medio e custo.`), nunca `unsupported_receipt`. METODO (ordem do CEO 2026-08-29): TODA bet da Polymarket e do metodo `ML`, qualquer que seja o mercado — grave `raw_extraction.method_tag: "ml"` alem do `market` literal do painel. A aba ML do dashboard classifica Polymarket pela CASA, nao pelo texto do mercado, entao nunca force o texto do `market` pra encaixar num balde: registre o mercado como ele aparece.
5. Antes de qualquer write, monte um unico JSON UTF-8 em `cron-data/bet-upload-work/<id>-batch.json` no formato `{ "items": [ ... ] }`, um item por card detectado, na MESMA ordem visual — nunca omita um card detectado: se ele e invalido, inclua-o como rejeitado (passo 6) em vez de deixar de fora do array. Cada item e OU `{ "bet": { ... } }` (card valido, payload completo) OU `{ "rejected": { "error_code": "...", "message": "..." } }` (card invalido). Entao execute exatamente uma vez:
   node scripts/bet-upload-jobs.cjs register-batch <id> <claim_token> cron-data/bet-upload-work/<id>-batch.json
   Esse comando valida os itens `bet` com `validateAndNormalize`, aplica o fair guard e chama a RPC transacional uma vez — ela registra os cards validos e devolve, por item, o motivo dos rejeitados. Nunca chame `supabase-save-bet.cjs` individualmente e nunca finalize `registered` pela RPC legada. Duplicatas confirmadas podem ser reutilizadas; ladders com pick/stake/mapa/odd diferentes permanecem registros separados.
6. Card invalido NAO aborta os demais: vira um item `{ "rejected": { "error_code": "...", "message": "Aposta N: motivo" } }` no MESMO `<id>-batch.json` do passo 5, na posicao visual dele. Use sempre um destes codigos da allowlist fechada — nunca invente outro:
   - `unsupported_bookmaker` -> APOSENTADO (fail-open 2026-08-17): NAO rejeite card por causa da casa; registre com `unknown`/nome-lido + `bookmaker_unverified` (ver REGRA CASA). Mantido na allowlist so por compatibilidade.
   - `unreadable_image` -> `Nao consegui ler o comprovante. Envie outro print mais claro.`
   - `unsupported_receipt` -> `Comprovante de aposta nao reconhecido.`
   - `too_many_bets` -> `O print tem mais de 10 apostas.`
   - `ambiguous_bet` -> `Nao consegui confirmar a partida ou o mapa desta aposta com seguranca.`
   - `duplicate_bet` -> `Esta aposta ja foi registrada antes.`
   - `extraction_failed` -> qualquer outra causa de rejeicao entra aqui, com mensagem curta e sanitizada coerente com o motivo real.
   So use `finish rejected` diretamente (SEM passar por `register-batch`) em duas situacoes de nivel de PRINT, nunca de card: (a) mais de 10 cards detectados (`too_many_bets`, ja coberto no passo 4) ou (b) nenhum card e identificavel na imagem inteira (`unreadable_image`/`unsupported_receipt`). Painel de prediction market (Polymarket) com posicao executada NUNCA cai em (b) — layout de trading nao e print ilegivel, ver a regra Polymarket no passo 4. Nesses dois casos:
   node scripts/bet-upload-jobs.cjs finish <id> <claim_token> rejected - <codigo> "<mensagem>" -
   Em qualquer outro cenario — inclusive quando TODOS os cards do print acabam invalidos — submeta o batch normalmente pelo passo 5: a RPC fecha o job como `rejected` sozinha quando zero cards sobrevivem, com o motivo de cada card auditado em `result.rejected_items`.
7. Falha tecnica antes da RPC: finalize error com mensagem sanitizada. Se o retorno da RPC for perdido/ambiguo, NAO repita save nem use finish; preserve arquivos para diagnostico, pois a transacao pode ter sido concluida.
8. Depois de estado terminal confirmado, execute exatamente:
   node scripts/bet-upload-jobs.cjs cleanup <id>
   Isso apaga somente os TEMPs exatos deste job (imagem, `<id>-batch.json` e `<id>-result.json`, quando existirem) dentro de `cron-data/bet-upload-work`. Nunca apague manualmente, nunca use curinga, `-Recurse` nem remova outro path. Nunca toque fora de `cron-data/bet-upload-work` e nunca apague Storage diretamente.
9. A resposta da RPC traz `total`, `inserted`, `reused` e todos os `bet_ids` dos cards REGISTRADOS, mais `rejected` e `rejected_items` dos cards descartados — sucesso parcial (alguns registrados, outros rejeitados no mesmo print) e o comportamento ESPERADO, nao um erro. So sao invalidos: batch vazio, mais de 10 itens, lease vencida ou claim divergente — nesses casos a RPC falha e voce nao finaliza `registered` nem `rejected` manualmente por cima dela. Nunca exponha tokens, secrets ou resposta crua com credenciais.

Compatibilidade e auditoria: Nunca finalize `registered` sem UUID real. O JSON minimo auditavel inclui a description recebida e somente campos publicos dos cards. A limpeza local ocorre apenas depois do estado terminal `registered`, `rejected` ou `error`. Nunca apague o objeto do Storage diretamente: somente o comando purge pode faze-lo ao fim da retencao privada de 14 dias.
